include modals/help-modal

nav.nav-extended
    div.nav-content
        ul.tabs.tabs-transparent
            if(currentPage == "/")
                li.tab
                    a.active(href="/", target="_self") Информация
            else
                li.tab
                    a(href="/", target="_self") Информация
            if(currentPage=="/rec")
                li.tab
                    a.active(href="/rec", target="_self") Разметка
            else
                li.tab
                    a(href="/rec", target="_self") Разметка
            if(currentPage == "/settings")
                li.tab
                    a.active(href="/settings", target="_self") Параметры
            else
                li.tab
                    a(href="/settings", target="_self") Параметры
            if(admin)
                if(currentPage == "/stats")
                    li.tab
                        a.active(href="/stats", target="_self") Статистика
                else
                    li.tab
                        a(href="/stats", target="_self") Статистика
            if(admin)
                if(currentPage == "/stats_cnn")
                    li.tab
                        a.active(href="/stats_cnn", target="_self") Статистика(НС)
                else
                    li.tab
                        a(href="/stats_cnn", target="_self") Статистика(НС)

            if(currentPage == "/help")
                li.tab
                    a.active.modal-trigger(href="#help-modal", target="_self") Помощь
            else
                li.tab
                    a.modal-trigger(href="#help-modal", target="_self") Помощь
            if(admin)
                if(currentPage == "/appoint")
                    li.tab
                        a.active.modal-trigger(href="/appoint", target="_self") Назначить
                else
                    li.tab
                        a.modal-trigger(href="/appoint", target="_self") Назначить
            if(admin)
                if(currentPage == "/users")
                    li.tab
                        a.active.modal-trigger(href="/users", target="_self") Пользователи
                else
                    li.tab
                        a.modal-trigger(href="/users", target="_self") Пользователи
            if(admin)
                if(currentPage == "/indexing")
                    li.tab
                        a.active.modal-trigger(href="/indexing", target="_self") Индексация
                else
                    li.tab
                        a.modal-trigger(href="/indexing", target="_self") Индексация
            if(currentPage == "/updates")
                li.tab
                    a.active.modal-trigger(href="/updates", target="_self") Обновления
            else
                li.tab
                    a.modal-trigger(href="/updates", target="_self") Обновления
            if(currentPage == "/logout")
                li.tab
                    a.active(href="/logout", target="_self") Выход
            else
                li.tab
                    a(href="/logout", target="_self") Выход

